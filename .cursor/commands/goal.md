# Goal

Follow `.cursor/skills/goal/SKILL.md`.

The user's goal is **everything after `/goal` on this line**. If empty, show usage.

Arm **dynamic loop** mode per the built-in **loop** skill. Run the goal once immediately, then continue until stop conditions in the goal skill.
